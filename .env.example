# Resend (required for contact form email delivery)
# Create an API key at https://resend.com/api-keys
RESEND_API_KEY=

# Contact form sender (optional — default shown)
# FROM must be on a domain verified in Resend
CONTACT_FROM_EMAIL="Green Expert <contact@greenexpert.ma>"
# Enquiries always go to said@greenexpert.ma (defined in app/api/contact/route.ts).
# Legacy CONTACT_TO_EMAIL overrides are intentionally ignored.

# Public site URL used in email templates (links back to the site)
NEXT_PUBLIC_BASE_URL=https://www.greenexpert.ma

# Optional: public HTML-tag token supplied by Google Search Console
GOOGLE_SITE_VERIFICATION=

# Optional: existing GA4 web-stream measurement ID (leave empty to disable analytics)
NEXT_PUBLIC_GA_MEASUREMENT_ID=
